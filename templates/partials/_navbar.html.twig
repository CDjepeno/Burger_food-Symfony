<nav class="navbar navbar-expand-lg navbar-light bg-primary">
	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<form class="form-inline my-2 my-lg-0 mr-auto" action="{{ path("product_search") }}" method="post">
			<input class="form-control mr-sm-2" name="search" type="search" placeholder="Produit" aria-label="Search">
			<button class="btn btn-outline-success my-2 my-sm-0" type="submit">Produit</button>
		</form>
		<ul class="navbar-nav pull-right">
			{% if not app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path("customer_register") }}">Inscription
						<span class="sr-only">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link btn btn-success" href="{{ path("customer_login") }}">Connexion</a>
				</li>
			{% endif %}
			{% if app.user %}	
				<li class="nav-item">
					<a class="nav-link btn btn-danger" href="{{ path("customer_logout") }}">Deconnexion</a>
				</li>
				<li class="nav-item">
					<a class="nav-link btn btn-info ml-1" href="{{ path("purchase_index") }}">Mes commande</a>
				</li>
			{% endif %}
			{% if is_granted("ROLE_ADMIN") %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
						Administration
					</a>
					<div class="dropdown-menu" aria-labelledby="navbarDropdown">
						<a class="dropdown-item" href="{{ path("admin_products") }}">Produit</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item" href="{{ path("admin_customers") }}">Client</a>
					</div>
				</li>
			{% endif %}
			<li class="nav-item">	
				<a class="nav-link" id="cart" href="{{ path("cart_show") }}"><i class="fi-xwsuhl-shopping-cart-solid"></i>Panier {{ cartService.total | amount }}</a>
			</li>
		</ul>


	</div>
</nav>
